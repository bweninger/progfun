package forcomp

import Anagrams._

object Anagramas {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(93); 
	
	
		val x = wordOccurrences("Bruno");System.out.println("""x  : forcomp.Anagrams.Occurrences = """ + $show(x ))}
		
			  
}
